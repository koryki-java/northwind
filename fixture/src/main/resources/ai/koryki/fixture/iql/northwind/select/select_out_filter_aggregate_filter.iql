// filter, aggregate, group and having

SELECT
    customers c
        OUT c.country country
        OUT count(c.customer_id) cnt
        FILTER c.company_name LIKE 'abc%'
        GROUP c.country
        HAVING count(c.customer_id) > 10