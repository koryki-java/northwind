// find customers, give company_name, filter by company_name

SELECT
    customers c
        OUT c.company_name
        FILTER c.company_name LIKE 'abc%' AND (c.customer_id < 10 OR c.city = 'Berlin')
