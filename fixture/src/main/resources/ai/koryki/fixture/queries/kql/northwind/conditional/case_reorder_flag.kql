// Flag products that need reordering: on-hand stock below the reorder level (case as a two-way
// ternary — case(condition, then, else)).

FIND products p
FETCH
  p.product_id ASC,
  p.product_name,
  case(p.units_in_stock < p.reorder_level, 'reorder', 'ok')  stock_status
