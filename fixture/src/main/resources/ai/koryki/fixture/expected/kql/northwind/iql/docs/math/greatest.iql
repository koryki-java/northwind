// greatest: the larger of two values in the same row.

SELECT
 order_details od
  OUT od.order_id 1
  OUT od.product_id 2
  OUT round(greatest(od.unit_price, 20), 2) at_least_twenty 3
  ORDER od.order_id ASC 1
  ORDER od.product_id ASC 2
LIMIT 20