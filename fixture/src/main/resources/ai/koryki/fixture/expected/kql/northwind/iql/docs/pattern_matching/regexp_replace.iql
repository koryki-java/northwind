// regexp_replace: strip non-digits from a phone number.
// Every match is replaced, on every dialect — the levelling that makes this sample mean what it says.

SELECT
 customers c
  OUT regexp_replace(c.phone, '[^0-9]', '') digits_only 1